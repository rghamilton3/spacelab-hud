import { Theme, HudAlert } from "theme.slint";

component AlertRow {
    in property <HudAlert> alert;

    property <color> sev-color: root.alert.severity == "critical" ? Theme.danger
                               : (root.alert.severity == "warning" ? Theme.warn : Theme.accent);

    Rectangle {
        background: Theme.surface;
        border-radius: 6px;
        clip: true;

        HorizontalLayout {
            spacing: 0px;

            Rectangle {
                width: 3px;
                background: sev-color;
            }

            VerticalLayout {
                padding: 12px;
                spacing: 4px;
                alignment: center;

                HorizontalLayout {
                    alignment: space-between;
                    Text {
                        text: root.alert.name;
                        color: sev-color;
                        font-size: 15px;
                        font-family: "monospace";
                    }
                    Text {
                        text: root.alert.age;
                        color: Theme.text-dim;
                        font-size: 12px;
                        vertical-alignment: center;
                    }
                }
                Text {
                    text: root.alert.summary;
                    color: Theme.text-primary;
                    font-size: 12px;
                }
                Text {
                    text: root.alert.severity;
                    color: sev-color;
                    font-size: 10px;
                    letter-spacing: 2px;
                }
            }
        }
    }
}

export component AlertsScreen {
    in property <[HudAlert]> alerts;
    in property <int>        alert-count;
    in property <int>        alert-worst-severity;
    in property <bool>       show-back:    false;
    in property <bool>       show-forward: false;

    property <int> level: alert-count == 0 ? 0 : alert-worst-severity;
    property <color> status-color:
        level == 2 ? Theme.danger : (level == 1 ? Theme.warn : Theme.accent);
    property <string> status-text:
        alert-count == 0 ? "NOMINAL"
        : (level == 2 ? "CRIT" : (level == 1 ? "WARN" : "ACTIVE"));

    VerticalLayout {
        padding: 0px;
        spacing: 0px;

        // ── Header ──────────────────────────────────────────────────
        Rectangle {
            height: 56px;
            background: Theme.surface;

            HorizontalLayout {
                padding-left: 14px;
                padding-right: 14px;
                alignment: space-between;

                HorizontalLayout {
                    spacing: 8px;
                    Text {
                        text: "‹";
                        color: Theme.text-label;
                        font-size: 28px;
                        vertical-alignment: center;
                        opacity: root.show-back ? 0.8 : 0.0;
                        animate opacity { duration: 200ms; }
                    }
                    HorizontalLayout {
                        spacing: 0px;
                        Text {
                            text: "SPACELAB-1 // ALERTS // ";
                            color: Theme.accent;
                            font-size: 17px;
                            letter-spacing: 3px;
                            vertical-alignment: center;
                        }
                        Text {
                            text: status-text;
                            color: status-color;
                            font-size: 17px;
                            letter-spacing: 3px;
                            vertical-alignment: center;
                            animate color { duration: 500ms; }
                        }
                    }
                }

                HorizontalLayout {
                    spacing: 8px;
                    Text {
                        text: root.alert-count == 0 ? "NOMINAL" : (root.alert-count + " ACTIVE");
                        color: root.alert-count == 0 ? Theme.accent : Theme.danger;
                        font-size: 13px;
                        letter-spacing: 2px;
                        vertical-alignment: center;
                    }
                    Text {
                        text: "›";
                        color: Theme.text-label;
                        font-size: 28px;
                        vertical-alignment: center;
                        opacity: root.show-forward ? 0.8 : 0.0;
                        animate opacity { duration: 200ms; }
                    }
                }
            }
        }

        Rectangle { height: 1px; background: Theme.border; }

        // ── No alerts ────────────────────────────────────────────────
        if root.alert-count == 0: Rectangle {
            VerticalLayout {
                alignment: center;
                spacing: 16px;

                Text {
                    text: "✓";
                    color: Theme.accent;
                    font-size: 48px;
                    horizontal-alignment: center;
                }
                Text {
                    text: "ALL SYSTEMS NOMINAL";
                    color: Theme.accent;
                    font-size: 18px;
                    letter-spacing: 3px;
                    horizontal-alignment: center;
                }
            }
        }

        // ── Active alerts ─────────────────────────────────────────────
        if root.alert-count > 0: VerticalLayout {
            padding: 10px;
            spacing: 7px;

            for alert in root.alerts: AlertRow { alert: alert; }
        }
    }
}
